# Plan to Modify Time Scaling Plots

**Date:** 22-04-2025 01:17:00

**Goal:** Modify the time scaling plots generated by `scripts/simstudy_2/analyze_optimization_results_main.py` to create three separate figures showing scaling with N, T, and K respectively, while holding other dimensions constant.

**Fixed Values:**
*   N = 10
*   T = 1000
*   K = 3

**Implementation Steps:**

1.  **Modify `scripts/simstudy_2/analysis_plotting_utils.py`:**
    *   **Function Signature:** Change `create_time_scaling_plots` to return `Dict[str, Figure]`.
    *   **Remove Subplots:** Delete the existing `fig, (ax1, ax2) = plt.subplots(1, 2, ...)` logic.
    *   **Create Figure 1 (Scaling vs. N):**
        *   Filter the input DataFrame `df_agg` for rows where `config_T == 1000` and `K == 3`.
        *   Create a new `matplotlib.figure.Figure`.
        *   Use `seaborn.lineplot` on the filtered data:
            *   `x="N"`
            *   `y="timing_total_script_duration_s_mean"`
            *   `hue="filter_config"` (using the established color palette)
            *   Use markers and distinct line styles if needed for clarity.
        *   Set the y-axis to log scale (`ax.set_yscale('log')`).
        *   Set a clear title: "Computation Time Scaling vs. N (Fixed T=1000, K=3)".
        *   Set appropriate x/y labels ("Number of Assets (N)", "Mean Computation Time (s, log scale)").
        *   Add a legend for `filter_config`.
        *   Apply publication style using `apply_publication_style()`.
        *   Store this figure in a dictionary with the key `"scaling_N"`.
    *   **Create Figure 2 (Scaling vs. T):**
        *   Filter `df_agg` for rows where `N == 10` and `K == 3`.
        *   Create a new `matplotlib.figure.Figure`.
        *   Use `seaborn.lineplot` on the filtered data:
            *   `x="config_T"`
            *   `y="timing_total_script_duration_s_mean"`
            *   `hue="filter_config"`
        *   Set the y-axis to log scale.
        *   Set a clear title: "Computation Time Scaling vs. T (Fixed N=10, K=3)".
        *   Set appropriate x/y labels ("Time Series Length (T)", "Mean Computation Time (s, log scale)").
        *   Add a legend.
        *   Apply publication style.
        *   Store this figure in a dictionary with the key `"scaling_T"`.
    *   **Create Figure 3 (Scaling vs. K):**
        *   Filter `df_agg` for rows where `N == 10` and `config_T == 1000`.
        *   Create a new `matplotlib.figure.Figure`.
        *   Use `seaborn.lineplot` on the filtered data:
            *   `x="K"`
            *   `y="timing_total_script_duration_s_mean"`
            *   `hue="filter_config"`
        *   Set the y-axis to log scale.
        *   Set a clear title: "Computation Time Scaling vs. K (Fixed N=10, T=1000)".
        *   Set appropriate x/y labels ("Number of Factors (K)", "Mean Computation Time (s, log scale)").
        *   Add a legend.
        *   Apply publication style.
        *   Store this figure in a dictionary with the key `"scaling_K"`.
    *   **Return Value:** Return the dictionary containing the three figures.
    *   **Docstring:** Update the function's docstring to reflect the new return type and behavior.

2.  **Modify `scripts/simstudy_2/analyze_optimization_results_main.py`:**
    *   **Update Call:** Change the line calling `create_time_scaling_plots` (around L229) to receive the dictionary (e.g., `scaling_figs_dict = create_time_scaling_plots(df_agg_scalars)`).
    *   **Update Saving Logic:** Replace the single `save_analysis_output` call for `scaling_fig` (around L230) with a loop:
        ```python
        # Example loop structure
        for key, figure in scaling_figs_dict.items():
            save_analysis_output(figure, f"time_{key}", output_dir)
            plt.close(figure) # Ensure figures are closed after saving
        ```
    *   Adjust logging messages related to time scaling plot generation and saving.